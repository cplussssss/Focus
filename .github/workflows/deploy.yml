name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Inject password
        run: sed -i "1s/^/const PASSWORD = '${{ secrets.TOEIC_PASSWORD }}';\n/" script.js

      - name: Inject Firebase config
        env:
          FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
          FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        run: |
          sed -i "s|'__FIREBASE_API_KEY__'|'${FIREBASE_API_KEY}'|g" auth.js
          sed -i "s|'__FIREBASE_AUTH_DOMAIN__'|'${FIREBASE_AUTH_DOMAIN}'|g" auth.js
          sed -i "s|'__FIREBASE_PROJECT_ID__'|'${FIREBASE_PROJECT_ID}'|g" auth.js
          sed -i "s|'__FIREBASE_STORAGE_BUCKET__'|'${FIREBASE_STORAGE_BUCKET}'|g" auth.js
          sed -i "s|'__FIREBASE_MESSAGING_SENDER_ID__'|'${FIREBASE_MESSAGING_SENDER_ID}'|g" auth.js
          sed -i "s|'__FIREBASE_APP_ID__'|'${FIREBASE_APP_ID}'|g" auth.js
            - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - uses: actions/deploy-pages@v4
        id: deployment

